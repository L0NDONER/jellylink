[DEFAULT]
[DEFAULT]
# JellyLink Configuration File
# Enable or disable dry-run mode (true = no changes made)
DRY_RUN = true

# Folder to watch for new downloads
WATCH_FOLDER = /home/martin/Downloads

# Root media directory
MEDIA_ROOT = /media

# Subfolders inside MEDIA_ROOT
TV_FOLDER = TV
MOVIE_FOLDER = Movies

# Skip sample files (true/false)
SKIP_SAMPLES = true

# Log file path
LOG_FILE = /home/martin/Desktop/python/jellylink.log

# Time (seconds) since last modification before processing a file
DOWNLOAD_GRACE_PERIOD = 60

# How often to scan for new files (seconds)
SCAN_INTERVAL = 15

# --- RECURSIVE SCANNING ---
# jellylink.py looks for RECURSIVE_SCAN
RECURSIVE_SCAN = true

# Keeping these for your own reference/future logic updates
SCAN_SUBFOLDERS = true
MAX_SUBFOLDER_DEPTH = 1

# --- NOTIFICATIONS (Optional) ---
ENABLE_WHATSAPP = false
TWILIO_ACCOUNT_SID = 
TWILIO_AUTH_TOKEN = 
TWILIO_WHATSAPP_FROM = +14155238886
WHATSAPP_TO =
